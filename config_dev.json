{ "PORT_HTTP"          :  81
, "PORT_HTTPS"         : 444
,  "KEY_PEM"           :    "../KEYSTORE/server/privkey.pem"
, "CERT_PEM"           :    "../KEYSTORE/server/fullchain.pem"

, "HOST"               : "localhost"
, "PORT"               : 5432
, "USER"               : "postgres"
, "PASSWORD"           : "postgres"
, "DATABASE"           : "postgres"

,   "TOPICS_TABLE"     : "topics"
,     "I18N_TABLE"     : "i18n"
, "FEEDBACK_TABLE"     : "feedbacks"

, "SELECT_feedbacks"   : "SELECT * FROM feedbacks WHERE user_id='?' ORDER BY subject,question"
, "SELECT_i18n"        : "SELECT * FROM i18n                        ORDER BY key"
, "SELECT_topics"      : "SELECT * FROM topics                      ORDER BY subject,question"

, "LOG_MORE"           : true
}
